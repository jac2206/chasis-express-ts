# Skill: Zod + OpenAPI

Usa `registerRoute` y el `registry` compartido, no documentación manual desconectada. Cada endpoint debe documentar schemas de entrada, salida, seguridad y errores relevantes. Verifica la UI en `/docs` tras cambios de contrato.

